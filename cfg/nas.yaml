# если таск называется с точки - он пропускается
#	Копируется на dst:/dir/hostd

tasks:

      local-to-nas-archive:
                  - purge:
                         do: true
                         period: 1D
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /data/archive
                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume2/raid1/backup


      local-to-nas-nextcloud:
                  - purge:
                         do: true
                         period: 1D
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /data3/nextcloud
                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume2/raid1/backup

      local-to-nas-data:
                  - purge:
                         do: true
                         period: 1D
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /data/data
                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume2/raid1/backup



      local-to-nas-photo:
                  - purge:
                         do: true
                         period: 6M
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /data/photo
                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume2/photo

      local-to-nas-sound:
                  - purge:
                         do: true
                         period: 3D
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /data/sound
                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume3/music


      local-to-nas-etc:
                  - purge:
                         do: true
                         period: 3M
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /etc
                           - /root
                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume3/data1/backup

      local-to-nas-srv:
                  - purge:
                         do: true
                         period: 3M
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /srv
                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume3/data1/backup




      local-to-nas-home:
                  - purge:
                         do: true
                         period: 3M
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /home
                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume3/data1/backup

      local-to-nas-opt:
                  - purge:
                         do: true
                         period: 3M
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /opt
                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume3/data1/backup



      .local-to-nas-down:
                  - purge:
                         do: true
                         period: 1D
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /data/down

                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume3/data1/backup

      .local-to-nas-temp:
                  - purge:
                         do: true
                         period: 1W
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /data/temp

                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume3/data1/backup


      local-to-nas-book:
                  - purge:
                         do: true
                         period: 1W
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /data/book

                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume3/data1/backup

      local-to-nas-distr:
                  - purge:
                         do: true
                         period: 3D
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /data/distr

                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume3/data1/backup

      .local-to-nas-video-1:
                  - purge:
                         do: true
                         period: 1D
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /data/video/Видеокурсы

                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume3/video


      local-to-nas-video-2:
                  - purge:
                         do: true
                         period: 1D
                  - src:
                           hostid: veter
                           host:
                           user:
                           dirs:
                           - /data/video/Кино

                  - dst:
                         host: 10.10.0.100
                         user: root
                         dir: /volume3/video
